{
  "name": "@smi/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "echo 'No build step required for API'",
    "lint": "eslint src --ext .js",
    "format": "prettier --write \"src/**/*.js\"",
    "clean": "echo 'Clean step not required'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "express-validator": "^7.2.0",
    "helmet": "^8.0.0",
    "nodemailer": "^6.9.15"
  },
  "devDependencies": {
    "eslint": "^9.9.1",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
